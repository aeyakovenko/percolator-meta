[package]
name = "auth-matcher"
version = "0.1.0"
edition = "2021"
publish = false

# Keep the test-only SBF fixture out of the production workspace.
[workspace]

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "=1.18.26"
